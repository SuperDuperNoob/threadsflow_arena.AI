node_modules/
.env
dist/
*.log
.DS_Store
ui/data/
logs/

# Uploaded PDFs stay in the repo (user pushed them intentionally), but any extracted
# working files do not.
Books/*.txt
Books/*.json

# Backup directory for n8n migration
infra/backups/
